{% extends 'base.html.twig' %}

{% block title %}Editer un lieu{% endblock %}

{% block body %}
    <h1 class="text-muted">Editer un site</h1>

    <div class="container-fluid">
        <div class="row">
            <div class="col-6">
                {{ form_start(form) }}
                {{ form_label(form.nomLieu) }}
                {{ form_widget(form.nomLieu) }}
                {{ form_errors(form.nomLieu) }}

                {{ form_label(form.rue) }}
                {{ form_widget(form.rue) }}
                {{ form_errors(form.rue) }}

                {{ form_label(form.ville) }}
                {{ form_widget(form.ville) }}
                {{ form_errors(form.ville) }}

                {{ form_label(form.latitude) }}
                {{ form_widget(form.latitude) }}
                {{ form_errors(form.latitude) }}

                {{ form_label(form.longitude) }}
                {{ form_widget(form.longitude) }}
                {{ form_errors(form.longitude) }}
            </div>
        </div>
        <br>
        <div class="row">

            <div class="col-3">
                {{ form_widget(form.submit) }}
                {{ form_end(form) }}
            </div>
            <div class="col-5">
                <a href="{{ path('lieu_index') }}">
                    <button class="btn bouton">Retour à la liste</button>
                </a>
            </div>

        </div>

    </div>

    </div>




{% endblock %}
